<%= if @event.attendees.any?%>
    <% @event.attendees.each do |attendee|%>
    <p>
        <%= attendee.name%>
    </p>
    <% end %>
<% else%>
    <p>No attendees</p>
<% end %>
<%= render 'attend_cancel' %>
